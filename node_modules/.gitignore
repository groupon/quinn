# Generally we want to ignore everything
*

# But it would be pointless if we would ignore this file itself
!.gitignore

# Until we publish the following as stand-alone packages, we'll keep them here
!quinn-respond
!quinn-router
!quinnc
